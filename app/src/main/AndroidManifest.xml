<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zsp.widget">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">
        <activity android:name="example.spruce.SpruceActivity" />
        <activity android:name="example.screen.ScreenActivity" />
        <activity android:name="example.SpannableStringActivity" />
        <activity android:name="example.dialog.DialogTwoActivity" />
        <activity android:name="example.dialog.DialogOneActivity" />
        <activity android:name="example.HomeActivity" />
        <activity android:name="example.LoginOneActivity" />
        <activity android:name="example.TimerActivity" />
        <activity android:name="example.SearchBoxOneActivity" />
        <activity android:name="example.SearchBoxTwoActivity" />
        <activity android:name="example.PictureActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>